# -----------------------------------------------------------------------------
# User configuration
#
s="$HOME/.appearance.sh" && [ -f "$s" ] && . "$s"
s="$HOME/.config-common.sh" && [ -f "$s" ] && . "$s"
s="$HOME/.config-work.sh" && [ -f "$s" ] && . "$s"
s="$HOME/.nix-profile/etc/profile.d/hm-session-vars.sh" && [ -f "$s" ] && . "$s"
unset s

# [ -z $DISPLAY ] && [ "$(tty)" = "/dev/tty1" ] && exec sway
